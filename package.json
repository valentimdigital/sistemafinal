{
  "name": "atendimento-ia",
  "version": "1.0.0",
  "description": "Sistema de Atendimento com IA",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:dashboard\" \"npm run dev:home\" \"npm run dev:discadora\"",
    "dev:backend": "cd backend && npm start",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:dashboard": "cd dashboard && npm run dev",
    "dev:home": "cd home-portal && npm run dev",
    "dev:discadora": "cd discadora/frontend && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd ../dashboard && npm install && cd ../home-portal && npm install && cd ../discadora/frontend && npm install"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
