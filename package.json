{
  "name": "sistemafinal",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd MODELO/backend && npm start",
    "build": "cd MODELO/backend && npm install",
    "postinstall": "cd MODELO/backend && npm install"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
